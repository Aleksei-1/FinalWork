#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
#include <conio.h>
#include <locale.h>

int main()
{
	setlocale(LC_ALL, "russian");

	char ch
	ch = getchar();
	// Очистка символа новой строки из буфера ввода
	getchar(); // Считываем и игнорируем символ новой строки
	// Вывод символа
	printf("Вы ввели символ: %c\n", ch);
	// Вывод кода символа
	printf("Код символа: %d\n", ch);

	char str[200]; // Массив для строки длиной до 200 символов
	// Ввод строки
	printf("Ваше имя и фамилия: ");
	fgets(str, sizeof(str), stdin); // Считываем строку, включая пробелы
	// Определение длины строки
	int length = strlen(str);
	// Убираем символ новой строки, если он присутствует
	length -= (length > 0 && str[length - 1] == '\n');
	// Прямой вывод строки и её длины
	str[length] = '\0'; // Убедимся, что строка завершается корректно
	printf("Ваша строка: %s\n", str); // Вывод строки
	printf("Длина строки: %d символов\n", length); // Вывод длины строки

	int positive, negative;
	// Ввод двух целых чисел
	printf("Введите положительное целое число: ");
	scanf("%d", &positive);
	printf("Введите отрицательное целое число: ");
	scanf("%d", &negative);
	// Вывод чисел в десятичном виде с различными форматами
	// Десятичный формат с минимальной шириной поля 10, выравнивание по правому краю
	printf("\nДесятичный формат:\n");
	printf("Положительное число (ширина 10): %10d\n", positive);
	printf("Отрицательное число (ширина 10): %10d\n", negative);
	// Десятичный формат с минимальной шириной поля 10, выравнивание по левому краю
	printf("Положительное число (ширина 10, выравнивание влево): %-10d\n", positive);
	printf("Отрицательное число (ширина 10, выравнивание влево): %-10d\n", negative);
	// Десятичный формат с минимальной шириной поля 10, с минимальной шириной поля
	printf("Положительное число: \t%d\n", positive);
	printf("Отрицательное число: \t%d\n", negative);
	// Шестнадцатеричный формат с минимальной шириной поля 10, выравнивание по правому краю
	printf("\nШестнадцатеричный формат:\n");
	printf("Положительное число (ширина 10): %10x\n", positive);
	printf("Отрицательное число (ширина 10): %10x\n", negative);
	// Шестнадцатеричный формат с минимальной шириной поля 10, выравнивание по левому краю
	printf("Положительное число (ширина 10, выравнивание влево): %-10x\n", positive);
	printf("Отрицательное число (ширина 10, выравнивание влево): %-10x\n", negative);
	// Шестнадцатеричный формат с минимальной шириной поля 10, с минимальной шириной поля
	printf("Положительное число: \t%x\n", positive);
	printf("Отрицательное число: \t%x\n", negative);

	float positive1, negative1;
	// Ввод двух чисел с плавающей точкой
	printf("Введите положительное число с плавающей точкой: ");
	scanf("%f", &positive1);
	printf("Введите отрицательное число с плавающей точкой: ");;
	scanf("%f", &negative1);
	// Вывод чисел в десятичной и экспоненциальной формах с заданной точностью
	printf("\nДесятичная форма (три знака после запятой):\n");
	printf("Положительное число: %.3f\n", positive1);
	printf("Отрицательное число: %.3f\n", negative1);
	printf("\nЭкспоненциальная форма (три знака после запятой):\n");
	printf("Положительное число: %.3e\n", positive1);
	printf("Отрицательное число: %.3e\n", negative1);

	unsigned int num;
	// Ввод числа
	printf("\nВведите беззнаковое целое число: ");
	scanf("%u", &num);
	// Вывод в десятичном формате
	printf("Десятичный формат: %u\n", num);
	// Вывод в восьмеричном формате
	printf("Восьмеричный формат: %o\n", num);
	// Вывод в шестнадцатеричном формате
	printf("Шестнадцатеричный формат: %x\n", num);

	return 0;
}
